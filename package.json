{
  "name": "isacars-clone",
  "version": "1.0.0",
  "description": "Full-stack ISACARS ride-hailing app with React and Node.js",
  "scripts": {
    "install-all": "cd server && npm install && cd ../client && npm install",
    "dev": "concurrently \"cd server && npm run dev\" \"cd client && npm run dev\"",
    "start-server": "cd server && npm start",
    "start-client": "cd client && npm run dev",
    "build-client": "cd client && npm run build",
    "build": "cd server && npm install && cd ../client && npm install && cd client && npm run build",
    "start": "cd server && npm start"
  },
  "keywords": ["isacars", "taxi", "ride-hailing", "react", "nodejs"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
